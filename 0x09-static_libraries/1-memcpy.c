#include "main.h"
/**
 *_memcpy - a function that copy memory area
 *@dest: memory where is stores
 *@src: memory where is copy
 *@n: number of bytes
 *
 *Return: copy memory with n bytes change
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	int j = 0;
	int i = b;

	for (; r < i; r++)
	{
		dest[r] = src[r];
		b--;
	}
	return (dest);
}
